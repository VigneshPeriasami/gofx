CREATE DATABASE IF NOT EXISTS `MYSQL_DATABASE`;
USE `MYSQL_DATABASE`;

CREATE TABLE companies (
    id int not NULL,
    ibans VARCHAR(255),
    name VARCHAR(255),
    address VARCHAR(255),
    UNIQUE (id)
);
